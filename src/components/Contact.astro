<section
  id="contacto"
  class="relative overflow-hidden bg-[#F4F6FB] px-6 py-20 md:py-28"
>
  <!-- Ambient blobs -->
  <div
    class="absolute -right-40 top-10 h-96 w-96 rounded-full bg-[#FBE8E2] blur-3xl pointer-events-none"
  >
  </div>
  <div
    class="absolute -left-40 bottom-10 h-80 w-80 rounded-full bg-[#E8EDF5] blur-3xl pointer-events-none"
  >
  </div>

  <div class="relative mx-auto max-w-6xl">
    <div class="contact-grid">
      <!-- Left: copy + trust -->
      <div>
        <span class="contact-label" data-reveal>Diagnóstico inicial</span>

        <h2 class="contact-title" data-reveal data-delay="100">
          ¿Tu alojamiento está listo para recibir más reservas directas?
        </h2>

        <p class="contact-sub" data-reveal data-delay="200">
          Revisamos tu presencia digital y te decimos qué puedes mejorar para
          que más clientes te encuentren, te escriban y reserven contigo.
          Sin costo, sin compromiso.
        </p>

        <!-- Trust items -->
        <div class="trust-list" data-reveal data-delay="300">
          <div class="trust-row">
            <div class="trust-check">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none">
                <polyline points="20 6 9 17 4 12" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </div>
            <div>
              <strong>Sin compromiso</strong>
              <span>Primero revisamos si podemos ayudarte.</span>
            </div>
          </div>
          <div class="trust-row">
            <div class="trust-check">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none">
                <polyline points="20 6 9 17 4 12" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </div>
            <div>
              <strong>Diagnóstico práctico</strong>
              <span>Hablamos de contacto, reservas y mejoras reales.</span>
            </div>
          </div>
          <div class="trust-row">
            <div class="trust-check">
              <svg width="13" height="13" viewBox="0 0 24 24" fill="none">
                <polyline points="20 6 9 17 4 12" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </div>
            <div>
              <strong>Respuesta rápida</strong>
              <span>Nos comunicamos en menos de 24 horas.</span>
            </div>
          </div>
        </div>

        <!-- WhatsApp direct -->
        <a
          href="https://wa.me/573105477540"
          target="_blank"
          rel="noopener noreferrer"
          class="whatsapp-btn"
          data-reveal
          data-delay="400"
        >
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path
              d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"
            ></path>
          </svg>
          Escríbenos por WhatsApp
        </a>

        <p class="whatsapp-note" data-reveal data-delay="400">
          O completa el formulario y te respondemos en menos de 24 horas.
        </p>
      </div>

      <!-- Right: form card -->
      <div class="form-wrap" data-reveal data-delay="200">
        <div class="form-inner">
          <div class="form-header">
            <p class="form-eyebrow">Diagnóstico inicial</p>
            <h3 class="form-title">Cuéntanos sobre tu alojamiento</h3>
            <p class="form-desc">
              Te responderemos con una recomendación clara según tu situación actual.
            </p>
          </div>

          <form class="contact-form">
            <div class="form-row">
              <label class="form-field">
                <span>Nombre</span>
                <input type="text" name="nombre" placeholder="Tu nombre" />
              </label>
              <label class="form-field">
                <span>Alojamiento</span>
                <input type="text" name="alojamiento" placeholder="Nombre del alojamiento" />
              </label>
            </div>

            <div class="form-row">
              <label class="form-field">
                <span>WhatsApp</span>
                <input type="tel" name="whatsapp" placeholder="+57 300 000 0000" />
              </label>
              <label class="form-field">
                <span>Plan de interés</span>
                <select name="plan">
                  <option>Presencia Digital</option>
                  <option>Optimización Comercial</option>
                  <option>Sistema de Ventas</option>
                  <option>No estoy seguro</option>
                </select>
              </label>
            </div>

            <label class="form-field">
              <span>¿Qué quieres mejorar?</span>
              <textarea
                name="mensaje"
                rows="4"
                placeholder="Ej: más contactos por WhatsApp, mejorar la web, recibir más reservas directas..."
              ></textarea>
            </label>

            <button type="submit" class="form-submit">
              Solicitar diagnóstico gratuito
            </button>

            <p class="form-note">
              Sin compromiso. Te respondemos en menos de 24 horas.
            </p>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* ── Layout ── */
  .contact-grid {
    display: grid;
    gap: 48px;
    grid-template-columns: 1fr 1fr;
    align-items: start;
  }

  @media (max-width: 900px) {
    .contact-grid {
      grid-template-columns: 1fr;
    }
  }

  /* ── Left column ── */
  .contact-label {
    display: inline-flex;
    border-radius: 999px;
    background: #fbe8e2;
    padding: 8px 18px;
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 11px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: #e8623a;
  }

  .contact-title {
    margin-top: 20px;
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: clamp(26px, 3.5vw, 40px);
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.03em;
    color: #2c3e55;
  }

  .contact-sub {
    margin-top: 18px;
    font-family: "Inter", sans-serif;
    font-size: 16px;
    line-height: 1.75;
    color: rgba(44, 62, 85, 0.72);
    max-width: 520px;
  }

  /* ── Trust list ── */
  .trust-list {
    margin-top: 32px;
    display: grid;
    gap: 14px;
  }

  .trust-row {
    display: flex;
    align-items: flex-start;
    gap: 14px;
    padding: 16px 20px;
    border-radius: 16px;
    background: white;
    border: 1px solid #e8edf5;
    box-shadow: 0 4px 16px rgba(27, 63, 110, 0.05);
  }

  .trust-check {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 50%;
    background: #fbe8e2;
    color: #e8623a;
    margin-top: 1px;
  }

  .trust-row strong {
    display: block;
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 15px;
    font-weight: 800;
    color: #1b3f6e;
  }

  .trust-row span {
    display: block;
    margin-top: 2px;
    font-family: "Inter", sans-serif;
    font-size: 13.5px;
    color: rgba(44, 62, 85, 0.68);
  }

  /* ── WhatsApp button ── */
  .whatsapp-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    margin-top: 28px;
    padding: 15px 24px;
    border-radius: 14px;
    background: #25d366;
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 14px;
    font-weight: 800;
    color: white;
    text-decoration: none;
    box-shadow: 0 10px 28px rgba(37, 211, 102, 0.28);
    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease,
      background 0.2s ease;
  }

  .whatsapp-btn:hover {
    background: #1db954;
    transform: translateY(-2px);
    box-shadow: 0 18px 40px rgba(37, 211, 102, 0.38);
  }

  .whatsapp-note {
    margin-top: 12px;
    font-family: "Inter", sans-serif;
    font-size: 13px;
    color: rgba(44, 62, 85, 0.58);
  }

  /* ── Right: form card ── */
  .form-wrap {
    border-radius: 28px;
    border: 1px solid #e8edf5;
    background: white;
    box-shadow:
      0 24px 64px rgba(27, 63, 110, 0.1),
      0 8px 24px rgba(27, 63, 110, 0.06);
    overflow: hidden;
  }

  .form-inner {
    padding: 36px 32px;
  }

  @media (max-width: 480px) {
    .form-inner {
      padding: 24px 20px;
    }
  }

  /* ── Form header ── */
  .form-eyebrow {
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 11px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: #e8623a;
    margin: 0 0 10px;
  }

  .form-title {
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 22px;
    font-weight: 800;
    letter-spacing: -0.025em;
    color: #1b3f6e;
    margin: 0 0 10px;
  }

  .form-desc {
    font-family: "Inter", sans-serif;
    font-size: 14px;
    line-height: 1.65;
    color: rgba(44, 62, 85, 0.68);
    margin: 0 0 28px;
  }

  /* ── Form elements ── */
  .contact-form {
    display: grid;
    gap: 14px;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 14px;
  }

  @media (max-width: 560px) {
    .form-row {
      grid-template-columns: 1fr;
    }
  }

  .form-field {
    display: grid;
    gap: 7px;
  }

  .form-field span {
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 12.5px;
    font-weight: 800;
    color: #1b3f6e;
  }

  .form-field input,
  .form-field select,
  .form-field textarea {
    width: 100%;
    border: 1.5px solid #e8edf5;
    border-radius: 12px;
    background: #f4f6fb;
    padding: 13px 15px;
    font-family: "Inter", sans-serif;
    font-size: 14px;
    color: #2c3e55;
    outline: none;
    box-sizing: border-box;
    transition:
      border-color 0.2s ease,
      background 0.2s ease,
      box-shadow 0.2s ease;
  }

  .form-field textarea {
    resize: vertical;
  }

  .form-field input:focus,
  .form-field select:focus,
  .form-field textarea:focus {
    background: white;
    border-color: rgba(232, 98, 58, 0.6);
    box-shadow: 0 0 0 4px rgba(232, 98, 58, 0.1);
  }

  .form-field input::placeholder,
  .form-field textarea::placeholder {
    color: rgba(44, 62, 85, 0.38);
  }

  /* ── Submit button ── */
  .form-submit {
    margin-top: 4px;
    width: 100%;
    border: 0;
    border-radius: 14px;
    background: #e8623a;
    padding: 16px 20px;
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 15px;
    font-weight: 800;
    color: white;
    cursor: pointer;
    letter-spacing: 0.01em;
    box-shadow: 0 12px 32px rgba(232, 98, 58, 0.28);
    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease,
      background 0.2s ease;
  }

  .form-submit:hover {
    background: #d4552f;
    transform: translateY(-2px);
    box-shadow: 0 18px 44px rgba(232, 98, 58, 0.38);
  }

  .form-submit:active {
    transform: translateY(0);
  }

  /* ── Form note ── */
  .form-note {
    text-align: center;
    font-family: "Inter", sans-serif;
    font-size: 12.5px;
    color: rgba(44, 62, 85, 0.55);
    margin: 0;
    line-height: 1.6;
  }
</style>

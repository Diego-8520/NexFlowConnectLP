<section
  id="contacto"
  class="relative overflow-hidden bg-[#1B3F6E] px-6 py-20 text-white md:py-28"
>
  <div
    class="absolute inset-0 -z-10 bg-[linear-gradient(135deg,#0F2440,#1B3F6E)]"
  >
  </div>
  <div
    class="absolute -right-40 top-10 -z-10 h-96 w-96 rounded-full bg-[#E8623A]/25 blur-3xl"
  >
  </div>
  <div
    class="absolute -left-40 bottom-10 -z-10 h-80 w-80 rounded-full bg-[#2A6BB5]/35 blur-3xl"
  >
  </div>

  <div
    class="absolute inset-0 -z-10 opacity-[0.07] bg-[linear-gradient(to_right,#ffffff_1px,transparent_1px),linear-gradient(to_bottom,#ffffff_1px,transparent_1px)] bg-[size:48px_48px]"
  >
  </div>

  <div
    class="mx-auto grid max-w-6xl gap-10 md:grid-cols-[0.95fr_1.05fr] md:items-center"
  >
    <div>
      <span
        class="reveal inline-flex rounded-full border border-white/15 bg-white/10 px-4 py-2 font-['Plus_Jakarta_Sans'] text-[11px] font-bold uppercase tracking-[0.18em] text-[#FBE8E2] backdrop-blur"
      >
        Contacto
      </span>

      <h2
        class="reveal delay-100 mt-5 font-['Plus_Jakarta_Sans'] text-3xl font-extrabold leading-tight tracking-[-0.03em] md:text-5xl"
      >
        Descubre dónde estás perdiendo reservas directas
      </h2>

      <p
        class="reveal delay-200 mt-5 max-w-xl font-['Inter'] text-base leading-8 text-white/78 md:text-lg"
      >
        Agenda un diagnóstico inicial y revisamos cómo está tu presencia
        digital, qué tan fácil es contactarte y qué mejoras pueden generar
        resultados visibles en pocos días.
      </p>

      <div class="reveal delay-300 mt-8 grid gap-3 sm:grid-cols-2">
        <div class="contact-benefit">
          <strong>Sin compromiso</strong>
          <span>Primero revisamos si podemos ayudarte.</span>
        </div>

        <div class="contact-benefit">
          <strong>Enfoque práctico</strong>
          <span>Hablamos de contacto, reservas y mejoras reales.</span>
        </div>
      </div>
    </div>

    <div class="reveal delay-200">
      <div
        class="rounded-[2rem] border border-white/15 bg-white/10 p-4 shadow-2xl backdrop-blur-xl sm:p-5"
      >
        <div class="rounded-[1.5rem] bg-[#F4F6FB] p-6 text-[#2C3E55] sm:p-8">
          <div>
            <p
              class="font-['Plus_Jakarta_Sans'] text-[11px] font-bold uppercase tracking-[0.18em] text-[#E8623A]"
            >
              Diagnóstico inicial
            </p>

            <h3
              class="mt-2 font-['Plus_Jakarta_Sans'] text-2xl font-extrabold tracking-[-0.02em] text-[#1B3F6E]"
            >
              Cuéntanos sobre tu alojamiento
            </h3>

            <p class="mt-3 font-['Inter'] text-sm leading-7 text-[#2C3E55]/70">
              Te responderemos con una recomendación clara según tu situación
              actual.
            </p>
          </div>

          <form class="mt-7 grid gap-4">
            <div class="grid gap-4 sm:grid-cols-2">
              <label class="field">
                <span>Nombre</span>
                <input type="text" name="nombre" placeholder="Tu nombre" />
              </label>

              <label class="field">
                <span>Alojamiento</span>
                <input
                  type="text"
                  name="alojamiento"
                  placeholder="Nombre del alojamiento"
                />
              </label>
            </div>

            <div class="grid gap-4 sm:grid-cols-2">
              <label class="field">
                <span>WhatsApp</span>
                <input
                  type="tel"
                  name="whatsapp"
                  placeholder="+57 300 000 0000"
                />
              </label>

              <label class="field">
                <span>Plan de interés</span>
                <select name="plan">
                  <option>Presencia Digital</option>
                  <option>Optimización Comercial</option>
                  <option>Sistema de Ventas</option>
                  <option>No estoy seguro</option>
                </select>
              </label>
            </div>

            <label class="field">
              <span>¿Qué quieres mejorar?</span>
              <textarea
                name="mensaje"
                rows="4"
                placeholder="Ej: más contactos por WhatsApp, mejorar la web, recibir más reservas directas..."
              ></textarea>
            </label>

            <button type="submit" class="submit-button">
              Solicitar diagnóstico
            </button>

            <p
              class="text-center font-['Inter'] text-xs leading-6 text-[#2C3E55]/60"
            >
              También puedes escribir directamente por WhatsApp si prefieres una
              respuesta más rápida.
            </p>
          </form>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-benefit {
    border-radius: 1.5rem;
    border: 1px solid rgba(255, 255, 255, 0.14);
    background: rgba(255, 255, 255, 0.08);
    padding: 18px;
    backdrop-filter: blur(14px);
  }

  .contact-benefit strong {
    display: block;
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 16px;
    font-weight: 800;
    color: white;
  }

  .contact-benefit span {
    display: block;
    margin-top: 6px;
    font-family: "Inter", sans-serif;
    font-size: 14px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.72);
  }

  .field {
    display: grid;
    gap: 8px;
  }

  .field span {
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 13px;
    font-weight: 800;
    color: #1b3f6e;
  }

  .field input,
  .field select,
  .field textarea {
    width: 100%;
    border: 1px solid #e8edf5;
    border-radius: 1rem;
    background: white;
    padding: 14px 15px;
    font-family: "Inter", sans-serif;
    font-size: 14px;
    color: #2c3e55;
    outline: none;
    transition:
      border-color 0.2s ease,
      box-shadow 0.2s ease,
      transform 0.2s ease;
  }

  .field textarea {
    resize: vertical;
  }

  .field input:focus,
  .field select:focus,
  .field textarea:focus {
    border-color: rgba(232, 98, 58, 0.65);
    box-shadow: 0 0 0 4px rgba(232, 98, 58, 0.12);
    transform: translateY(-1px);
  }

  .submit-button {
    margin-top: 4px;
    border: 0;
    border-radius: 1.1rem;
    background: #e8623a;
    padding: 16px 20px;
    font-family: "Plus Jakarta Sans", sans-serif;
    font-size: 14px;
    font-weight: 800;
    color: white;
    cursor: pointer;
    box-shadow: 0 16px 34px rgba(232, 98, 58, 0.28);
    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease;
  }

  .submit-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 20px 44px rgba(232, 98, 58, 0.38);
  }

  .reveal {
    animation: revealUp 0.8s ease both;
  }

  .delay-100 {
    animation-delay: 0.1s;
  }

  .delay-200 {
    animation-delay: 0.2s;
  }

  .delay-300 {
    animation-delay: 0.3s;
  }

  @keyframes revealUp {
    from {
      opacity: 0;
      transform: translateY(18px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal,
    .submit-button,
    .field input,
    .field select,
    .field textarea {
      animation: none;
      transition: none;
    }
  }
</style>
